#include <stdio.h>

/**
 * main - function starts
 * @argc: offset count
 * @argv: offset value
 * Return: 0 is sucess
 */

int main(int argc, char *argv[])
{
	_putchar("%s\n", argv[argc - 1]);
	return (0);
}

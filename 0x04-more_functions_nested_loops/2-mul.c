#include "main.h"
/**
 * mul -multiplier
 * @a: represents int variable
 * @b: represents 2nd int variable
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	mul = (a * b);

	return (mul);
}
